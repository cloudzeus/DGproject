// Stub — replaced by the full DeepSeek triage engine (plan Task 6).
export async function analyzeTicket(ticketId: string): Promise<void> {
  console.warn(`[tickets] triage not yet implemented, ticket ${ticketId} left in status=new`)
}
